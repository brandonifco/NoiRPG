# Superseded source text — NOT our rules source.
# BRP SRD 1.0.2 (2020, BRP-OGL) has a different resolution table and only four
# success grades. An agent that reads it will derive the wrong formulas.
# See docs/decisions/0001-source-text.md
BRP SRD 1.0.2.pdf

# .NET
bin/
obj/
*.user
[Dd]ebug/
[Rr]elease/
artifacts/
TestResults/
*.trx

# Python (tools/*.py, .github/workflows/*.py)
__pycache__/
*.py[cod]

# Dispatched-agent worktrees (tools/dispatch-agent.sh). Each is a full linked
# checkout under the primary tree; it must never show up as untracked here.
.worktrees/

# Editors
.vs/
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Auto-generated Codex role mirror (external tool derives it from .claude/agents/;
# the repo tracks tools/codex-agent.sh instead — see #142/#161).
.codex/
